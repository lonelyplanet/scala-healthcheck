service {
  name = "scala-healthcheck"
  environment = "test"
  environment = ${?ENVIRONMENT}
  group-id = "scala"
  group-id = ${?LP_SERVICE_GROUP_ID}
  owner-slack-id = "@wlk"
  owner-slack-id = ${?LP_SERVICE_OWNER_SLACK_ID}
  slack-channel = "#testing"
  slack-channer = ${?LP_SERVICE_SLACK_CHANNEL}
  github-commit = "unknown"
  github-commit = ${?GITHUB_COMMIT}
  github-repo = "scala-healthcheck"
  github-repo = ${?GITHUB_REPO}
  docker-image = "unknown"
  docker-image = ${?DOCKER_IMAGE}
}
